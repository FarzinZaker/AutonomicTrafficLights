package trafficLightSystem

import java.util.UUID

/**
  * Created by root on 4/4/16.
  */
class TestResult(val adaptationGroupId: UUID, val adaptationPathSourceDirection: Direction.Value, val adaptationPathDestinationDirection: Direction.Value, val adaptationFactor: Double, val value: Double) {

}
